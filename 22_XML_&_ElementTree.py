# Begin by visiting the documentation here: https://docs.python.org/
    # Select "Global Module Index"
    # Scroll down to "xml" and expand it to reveal " xml.etree.ElementTree"

# Open up your shell/terminal and type "import xml.etree.ElementTree as ET"
import xml.etree.ElementTree as ET
print(dir(ET))  # This shows a wall of text that isn't easy on the eyes
from inspect import getmembers, isclass, isfunction

